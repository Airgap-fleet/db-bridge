{
  "dxt_version": "0.1",
  "name": "postgresql-mcp",
  "display_name": "PostgreSQL MCP",
  "version": "1.0.0",
  "description": "PostgreSQL database operations for AI agents — query, execute, migrations, and EXPLAIN ANALYZE via FastMCP. Air-gapped, local-first, production-ready.",
  "long_description": "PostgreSQL MCP provides secure, local-only database access for AI agents. Built for regulated industries (finance, legal, marine) where data never leaves your infrastructure.\n\nFeatures:\n- Parameterized SELECT queries with result sets\n- DML execution (INSERT/UPDATE/DELETE) with row counts\n- Schema inspection: list tables, describe columns/indexes/constraints\n- Migration runner for schema changes\n- EXPLAIN ANALYZE for query optimization\n- Connection pooling (asyncpg) with configurable limits\n- Structured JSON logging via structlog\n- API key authentication for remote access\n- Multiple transports: stdio (default), SSE, Streamable HTTP\n- Runs on commodity hardware (AMD/DirectML optimized)\n- Zero cloud dependencies — fully air-gapped",
  "author": {
    "name": "AFaaS",
    "email": "mcp@afaas.io",
    "url": "https://github.com/afaas/postgresql-mcp"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/afaas/postgresql-mcp"
  },
  "homepage": "https://github.com/afaas/postgresql-mcp",
  "icon": "icon.png",
  "screenshots": [],
  "categories": ["developer-tools", "database", "data-analysis"],
  "keywords": ["postgresql", "mcp", "database", "sql", "local-first", "air-gapped", "analytics"],
  "mcp": {
    "server": {
      "command": "postgresql-mcp",
      "args": [],
      "env": {
        "DB_BRIDGE_DSN": {
          "type": "string",
          "description": "PostgreSQL connection string (postgresql://user:pass@host:port/db). Legacy POSTGRES_DSN / POSTGRESQL_MCP_DSN are accepted when unset.",
          "required": true
        },
        "DB_BRIDGE_LOG_LEVEL": {
          "type": "string",
          "description": "Logging level (stderr only)",
          "default": "INFO",
          "enum": ["DEBUG", "INFO", "WARNING", "ERROR"]
        },
        "DB_BRIDGE_POOL_SIZE": {
          "type": "number",
          "description": "Connection pool size",
          "default": 10
        },
        "DB_BRIDGE_QUERY_TIMEOUT": {
          "type": "number",
          "description": "Query timeout in seconds",
          "default": 30
        }
      }
    }
  },
  "runtime": {
    "type": "python",
    "version": ">=3.11",
    "pip_packages": ["postgresql-mcp==1.0.0"]
  },
  "compatibility": {
    "claude_desktop": ">=0.8.0",
    "platforms": ["darwin", "win32", "linux"]
  }
}