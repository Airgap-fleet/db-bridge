# Version note

| Field | Value |
|-------|--------|
| Package name | `airgap-db-bridge` |
| Version | **1.0.3** (source of truth: `pyproject.toml`) |
| Import package | `postgresql_mcp` |
| Console script | `airgap-db-bridge` |
| Build class | UNSIGNED INTERNAL |
| Thumbprint | (none — unsigned) |

Do not publish or upload wheels/sdists named `db_bridge-1.0.0*` or `db-bridge-1.0.0*`. Those names do not match this package.

Rebuild from a clean tree (`python -m build`) if you need a 1.0.3 artefact. Keep `dist/` out of git.
